---
server:
  port: 9090
spring:
  kafka:
    consumer:
      auto-offset-reset: earliest
      bootstrap-servers: localhost:9092
  jpa:
    hibernate:
      ddl-auto: create
logging:
  level:
    com:
      example:
        kafkaconsumer: info